[project]
name = "math_infer_finetune"
version = "0.1.0"
description = ""
authors = [{ name = "fe-bit" }]
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "dotenv (>=0.9.9,<0.10.0)",
    "langgraph (>=0.4.8,<0.5.0)",
    "transformers (>=4.52.2)",
    "openpyxl (>=3.1.5,<4.0.0)",
    "ipykernel (>=6.29.5,<7.0.0)",
    "huggingface-hub (>=0.30.0)",
    "python-dotenv (>=1.1.0,<2.0.0)",
    "torch (>=2.7.1,<3.0.0)",
    "seaborn (>=0.13.2,<0.14.0)",
    "chromadb (>=1.0.15,<2.0.0)",
    "matplotlib (>=3.10.3,<4.0.0)",
    "scikit-learn (>=1.7.0,<2.0.0)",
    "bert-score (>=0.3.13,<0.4.0)",
    "outlines (>=1.2.3,<2.0.0)",
    "jupyter (>=1.1.1,<2.0.0)",
    "ipywidgets (>=8.1.7,<9.0.0)",
    "math-datasets @ file:///C:/Users/patri/OneDrive/Dokumente/MA/code/math-infer-finetune/math-datasets",
]

[tool.poetry.dependencies]
outlines = { version = ">=1.2.3,<2.0.0", python = ">=3.11,<3.13" }
math-datasets = {develop = true}

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
